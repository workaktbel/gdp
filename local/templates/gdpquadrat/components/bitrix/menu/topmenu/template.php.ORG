<?if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED!==true)die();?>

<?if (!empty($arResult)):?>
    <div class="menu_top">
        <ul>
            <?
            foreach($arResult as $key => $arItem):
                if($arParams["MAX_LEVEL"] == 1 && $arItem["DEPTH_LEVEL"] > 1) continue;

                $active = "t-active";
                if ($arItem["TEXT"] == "Видео" || $arItem["TEXT"] == "Video"){
                    $class = " video";
                    $active = "";
                }
//echo "<pre>"; print_r($arItem["PARAMS"]["class"]);
                ?>
                <?if($arItem["SELECTED"]):?>
                <li class="<?=$arItem["PARAMS"]["class"]?> opened<?=$class?>">
                    <a href="<?=$arItem["LINK"]?>" data-menu-submenu-hook="" data-menu-item-number="<?=$key?>"><?=$arItem["TEXT"]?></a>
                </li>
            <?else:?>
                <li class=""><a class="<?=$arItem["PARAMS"]["class"]?>" href="<?=$arItem["LINK"]?>" data-menu-submenu-hook="" data-menu-item-number="<?=$key?>"><?=$arItem["TEXT"]?></a></li>
            <?endif?>
            <?endforeach?>
        </ul>
    </div>
<?endif?>

<style>
    .video{
        background: #0a007f !important;
    }
	.blue.opened.video {
		padding: 0;
		margin-left: 30px;
	}
	.blue.opened.video a {
		color: #fff !important;
		opacity: 1 !important;
		font-size: 18px;
		font-weight: 600;
		display: block;
    	text-decoration: none;
    	padding: 3px 15px;
	}
</style>

